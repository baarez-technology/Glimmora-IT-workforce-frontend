'use client';

import { DataTransfer } from '@/components/platform/data-transfer';

/** Phase 12 — Excel import with validate/preview/commit, and export. */
export default function DataTransferPage() {
  return <DataTransfer />;
}
