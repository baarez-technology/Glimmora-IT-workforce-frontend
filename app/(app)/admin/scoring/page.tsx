'use client';

import { ScoringRules } from '@/components/matching/scoring-rules';

/** Phase 7 — versioned matching weights. Phase 9 adds the other rule sets. */
export default function ScoringRulesPage() {
  return <ScoringRules />;
}
