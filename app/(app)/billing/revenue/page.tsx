'use client';

import { BillingWorkbench } from '@/components/delivery/billing';

/** Phase 11 — monthly billable revenue generated through the engine. */
export default function RevenuePage() {
  return <BillingWorkbench view="revenue" />;
}
