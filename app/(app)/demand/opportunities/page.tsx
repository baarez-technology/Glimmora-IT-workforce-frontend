'use client';

import { PipelineBoard } from '@/components/pipeline/pipeline-board';

/** Phase 10 — the same board, reached from Demand. */
export default function OpportunitiesPage() {
  return <PipelineBoard />;
}
