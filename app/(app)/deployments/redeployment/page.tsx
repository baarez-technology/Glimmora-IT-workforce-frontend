'use client';

import { BenchRadar } from '@/components/matching/bench-radar';

/** Phase 8 — unbilled capacity, and who has nowhere identified to go. */
export default function RedeploymentPage() {
  return <BenchRadar />;
}
