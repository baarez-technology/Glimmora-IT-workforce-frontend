'use client';

import { CommercialCalculator } from '@/components/scoring/commercial-calculator';

/** Phase 9 — the what-if commercial calculator. Persists nothing. */
export default function CommercialPage() {
  return <CommercialCalculator />;
}
