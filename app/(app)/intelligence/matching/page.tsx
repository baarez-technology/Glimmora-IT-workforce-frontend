'use client';

import { MatchingWorkbench } from '@/components/matching/matching-workbench';

/** Phase 7 — Demand → Resource matching with a full explanation per match. */
export default function MatchingPage() {
  return <MatchingWorkbench />;
}
