'use client';

import { OpportunityBoard } from '@/components/scoring/opportunity-board';

/** Phase 9 — Talent + Addressability + Commercial = Opportunity Score. */
export default function ScoringPage() {
  return <OpportunityBoard />;
}
