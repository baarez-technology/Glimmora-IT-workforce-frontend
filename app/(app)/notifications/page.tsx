'use client';

import { Notifications } from '@/components/platform/notifications';

/** Phase 12 — SLA, expiry, bench and interview alerts. */
export default function NotificationsPage() {
  return <Notifications />;
}
