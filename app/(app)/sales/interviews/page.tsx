'use client';

import { Interviews } from '@/components/pipeline/interviews';

/** Phase 10 — scheduled interviews and outcomes. */
export default function InterviewsPage() {
  return <Interviews />;
}
