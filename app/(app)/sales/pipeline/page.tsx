'use client';

import { PipelineBoard } from '@/components/pipeline/pipeline-board';

/** Phase 10 — the 12-stage sales pipeline. */
export default function PipelinePage() {
  return <PipelineBoard />;
}
