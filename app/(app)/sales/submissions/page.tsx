'use client';

import { Submissions } from '@/components/pipeline/submissions';

/** Phase 10 — CVs put forward, with duplicate protection. */
export default function SubmissionsPage() {
  return <Submissions />;
}
